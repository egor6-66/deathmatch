import Basic from './basic';

class FireArms extends Basic {
    // recoil: number;
    // constructor({ ...props, recoil }) {
    //     super(props);
    //     this.recoil = recoil;
    // }
}

export default FireArms;
